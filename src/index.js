import Autenticacao from "./Autenticacao";
import {
  isAuthenticated,
  getToken,
  getData,
  logout,
  login
} from "./services/auth";
export { Autenticacao, isAuthenticated, getToken, getData, logout, login };
